{
  "name": "my-app",
  "scripts": {
   "start": "concurrently \"npm run docker --prefix backend\" \"npm start --prefix backend\" \"npm run dev --prefix frontend\"",
    "docker": "cd backend && docker compose up -d"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
